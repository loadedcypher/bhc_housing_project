import 'package:flutter/material.dart';

class GeneralEnquiries extends StatelessWidget {
  const GeneralEnquiries({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Enquries');
  }
}
